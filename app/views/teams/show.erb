<!DOCTYPE html>
<html>
 <head>
  <meta charset="UTF-8">
  <title><%= @team.name "(#{@team.wins}-#{@team.losses})" %></title>
 </head>
 <body>
   <h1>League Name: <%= "#{@team}" %></h2>
   <h2>Quarterback: <%= "#{@team}" %></h2>
   <h2>Running Back: <%= "#{@team}" %></h2>
   <h2>Running Back: <%= "#{@team}" %></h2>
   <h2>Wide Receiver: <%= "#{@team}" %></h2>
   <h2>Wide Receiver: <%= "#{@team}" %></h2>
   <h2>Tight End <%= "#{@team}" %></h2>
   <h2>Kicker: <%= "#{@team}" %></h2>
   <h2>Defense: <%= "#{@team}" %></h2>
 </body>
</html>
